receivers:
  rabbitmq:
    endpoint: localhost:3306
    username: otel
    password: $RABBITMQ_PASSWORD
    collection_interval: 10s

processors:
  nop:

exporters:
  nop:

service:
  pipelines:
    metrics:
     receivers: [rabbitmq]
     processors: [nop]
     exporters: [nop]
