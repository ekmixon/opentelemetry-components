receivers:
  mysql:
    collection_interval: 10s
    username: otel
    password: otel
    database: otel
    endpoint: localhost:3306

processors:


exporters:
  logging:   
    loglevel: debug

service:
  pipelines:
    metrics:
     receivers: [mysql]
     processors: []
     exporters: [logging]
