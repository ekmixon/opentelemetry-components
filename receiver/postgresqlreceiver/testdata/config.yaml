receivers:
  postgresql:
    collection_interval: 10s
    username: otel
    password: otel
    database: otel
    endpoint: 127.0.0.1:5432

processors:

exporters:
    loglevel: debug

service:
  pipelines:
    metrics:
     receivers: [postgresql]
     processors:
     exporters: [logging]
